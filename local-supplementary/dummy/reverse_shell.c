#include <stdio.h>

int main() {
    // Hardcoded values for a and b
    int a = 5;
    int b = 7;
    
    // Perform addition
    int c = a + b;
    
    // Output the result
    // printf("The result of %d + %d = %d\n", a, b, c);
    
    return 0;
}
